@model IEnumerable<HelpDeskMvc.Models.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.dsDpto)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dsDpto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idDpto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idDpto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idDpto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
